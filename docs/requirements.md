# Requirements and grounding

## Current scope — September 7, 2026 (supersedes recommendation-only restriction)

The running internship Doc was reread through Google Drive, read-only. Its private
content is not copied here. The original research assignment and subsequent
workload-specific model exploration motivate the product; the user's current
instruction separately authorizes a workflow-aware **sandbox** upgrade. Neither
the assignment nor this milestone establishes model quality or production approval.

Milestone 06 audits the integrated alpha and delivers shared executable contracts,
revision-3 migrations and independent lane boundaries. It does not complete or
launch lanes 7/8. Product path: describe → clarify → propose/edit stages and
configurations → compare real outputs → sandbox-enable a pinned route → call a
single stage API or supported sample workflow → inspect outputs/usage.

Sandbox drafts may have untested quality, visibly provisional. Server-side license,
weights access, privacy, capabilities, credentials, authorization and spending
checks remain mandatory; unknown hard requirements fail closed. Existing strict
recommendation selection stays intact. A sandbox candidate for testing is not a
production migration recommendation. See `upgrades/router-v2.md` for exact rules.

Production routing, public deployment, hosted charges without explicit approvals,
new accounts/resources and customer business-tool side effects remain prohibited.
The historical foundation paragraphs below describe the original milestone only.

Verified September 6, 2026 during foundation work.

## Confirmed scope

The user's implementation request is the governing milestone: typed workflow
intake, open-weight configuration research/selection interfaces, evidence-backed
recommendations, controlled evaluation interfaces and **inactive draft policies**.
The foundation must be offline, explicitly synthetic, durable, tested and suitable
for two independent coding lanes. It must not execute customer business actions.

The named running internship document was successfully read through the Google
Drive connector. Its current instructions were checked for consistency with this
scope. Its private contents are not copied into the repository. Older research
recommendations are not new product requirements or verified model catalog data.

## Missing attachment

`buildbox-router-planning-pack.zip`, an extracted planning pack, and
`buildbox-open-weight-router-master-plan.md` were not found in the selected
workspace, Downloads, the surrounding ChatGPT document workspace, or the scoped
Codex attachment-name search. No planning pack was read or imported. If supplied
later, reconcile it with the contracts through the integration owner.

## Repository evidence

At intake this repository had no commits, no remotes, no root/ancestor AGENTS.md,
and no application manifests, tests or deployment configuration. It contained
untracked internship research. No reusable company application stack was present
in the selected repository; unrelated sibling projects were not reused or edited.
The new implementation is isolated under `router/` on `codex/router-foundation`.

## Implementation proposals, not discovered requirements

- React/Vite UI and FastAPI/Pydantic backend, selected using the greenfield stack
  specified by the user. PostgreSQL is the intended deployment database.
- SQLite is an explicit offline fixture storage option, not a hidden production
  database substitution. Docker's existing image-store I/O error prevents the
  local PostgreSQL run on this host; this is recorded, not repaired destructively.
- One worker in the backend package processes persisted recommendation jobs.
- Version `1.0` contracts and a single synthetic document-triage example establish
  boundaries; model names and metrics from previous chats are not imported.
- Live interpretation, research, evaluation, real authentication and deployment
  are intentionally disabled pending authorized follow-on work.
